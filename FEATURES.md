# 世界之眼 v4 — 功能说明

对应 `metadata.json` 版本 **4.0.0**，与 `index.js` / `sub-agent.js` 实现一致。

---

## 群公告版（约 10 条，可复制转发）

1. **世界之眼 v4**：把勾选「代理」的插件工具从主 AI 面前收走，换成少数总控工具，省 Token、好调度。
2. **主 AI 五件套**：`goal` 交目标、`delegate` 点名插件、`research` 专题研究、`inspect` 查完整工具签名、`control` 查状态/取结果/停止/**补答续跑**。
3. **v4 提速**：删除每步的动漫人格演出与思考独白；协商默认关闭；研究链路按深度分级压缩；点名委派直通执行。标准搜索/研究的模型往返从十几次降到 2-4 次。
4. **统一人格**：所有分身共用「细致、准确、克制」的工作人格——参数补全到位、结果带具体数值/路径/来源、零废话。
5. **搜索并行**：搜索阶段由单个子代理合并全部搜索插件工具，首轮一次性并行调用多引擎交叉取证。
6. **任务受阻直连主 AI**：缺名称/未登录/前置条件缺失时任务转 `waiting_input` 并推送求助通知，主 AI 从对话上下文补齐后用 `answer` 动作续跑，不用等用户。
7. **缩写声明**：主 AI 看到的插件清单每行都标注为「极简缩写」，能力边界以 `world_eye_inspect` 为准。
8. **同步 / 异步**：任务异步执行拿 task_id，结果在后台完成时自动推送。
9. **安全**：代码任务可单独限并发、限插件、拦截描述里的高危命令模式；URL/网页任务自动纠偏到 skills 类插件。
10. **详细表格**：见下文各节。

---

## 一、对主对话里的 AI 做了什么

| 功能 | 说明 |
|------|------|
| **工具瘦身** | 在设置中勾选「被代理」的插件后，这些插件的**原始工具**不再整包出现在主 AI 工具列表中，改由世界之眼注入少量元工具。 |
| **历史清理** | 清理对话里与已代理插件相关的旧工具调用痕迹，减轻主上下文干扰。 |
| **缩写声明** | 注入的插件清单每行明确标注为「极简缩写」，并把「先 inspect 再点名」写成硬规则。 |
| **结果/受阻推送** | 异步任务完成或受阻时，通过主对话消息通道自动推送；推送文案指示主 AI 直接讲重点、自主补答，不向用户征求许可。 |

---

## 二、主 AI 可用的 5 个工具

| 工具名 | 功能 |
|--------|------|
| **`world_eye_goal`** | 提交自然语言目标（单步或多步复合）；自动选工作流；识别为研究时可带深度与输出类型。 |
| **`world_eye_delegate`** | **指定插件名 + 任务描述**；plugin_name 有效时**直通执行**（跳过路由模型，仅保留浏览器/URL 安全纠偏）；无效或省略时回退目标路由。 |
| **`world_eye_research`** | **专题研究**：按深度分级执行（见下表）；研究用工具会过滤明显高危类工具名前缀。 |
| **`world_eye_inspect`** | 查看代理插件的**完整工具签名**（清单行只是缩写）；点名委派前必查。 |
| **`world_eye_control`** | **status / result / raw_result**：状态与结果；**list / queue**：任务与排队；**stop / cancel**：停止；**answer**：对 `waiting_input` 受阻任务补充信息并自动续跑。 |

### `agent_role` 可选值（delegate）

`general`、`planner`、`search`、`music`、`image`、`video`、`code`、`file`、`app`、`skills`、`reviewer`、`reporter`、`synthesizer`、`persona`（未指定时按插件推断）。

---

## 三、研究链路（v4 按深度分级）

| 深度 | 链路 | 子代理运行次数 |
|------|------|----------------|
| **quick** | 合并搜索（≤2 轮）→ 摘要式报告 | 2 |
| **standard**（默认） | 合并搜索（≤3 轮）→ 审查+撰写合并（自查发现严重缺口时输出【需要补搜】→ 1 次补搜 → 终稿） | 2（最多 4） |
| **deep** | 规划 → 合并搜索 → 独立审查 → 补搜循环（≤2 轮）→ 报告 | 4（最多 8） |

- 搜索阶段统一为 **1 个子代理合并全部搜索插件工具**，首轮强制并行调用多引擎。
- `output` 默认 **report**；`summary` 出摘要；`report+persona` 额外做一步**口语化改写**（事实不变，仅显式传参时执行）。
- 新研究/搜索任务提交时自动中止仍在运行、排队或受阻的旧搜索任务。

## 四、其他工作流类型

| 类型 | 作用 |
|------|------|
| **image_task / video_task / music_task / file_task / app_task / code_task** | 单插件类目标路由（code 受更严并发与安全限制）。 |
| **composite_task** | 预设复合多步（如小红书：撰文 + 生图 + 走 Skills 发布），支持步骤依赖与并行。 |
| **planned_composite** | 无现成模板时自动规划并并行拆步执行。 |
| **auto_goal** | 由高层目标自动落入上述某一类工作流。 |

---

## 五、下级智能体（Sub-Agent，v4）

- 使用独立 LLM 配置（或回退主应用）发起 HTTP 请求，按轮次 **Function Calling** 直至完成或达到上限。
- **统一工作人格**（可在配置关闭）：细致（参数补全）、准确（结果带数值/路径/来源）、克制（零演出零独白）、高效（能并行不串行）。
- **按角色**使用不同职责规则块（规划、搜索、审查、报告、口语化改写、代码、音乐等，见 `ROLE_PROMPT_BLOCKS`）。
- **共享执行规则**：优先工具取事实、禁止伪造结果、输出零角色扮演腔、阻塞需说明原因等（见 `sub-agent.js`）。

---

## 六、任务与调度

- 任务 **ID**、状态（pending / running / completed / failed / cancelled / **waiting_input**）。
- **waiting_input**：信息缺口类失败（名称未匹配、未登录、前置条件缺失、参数/路径不明）不直接失败，转入等待并推送求助通知；主 AI 用 `answer` 补充后自动续跑（attempt+1）；等待超 10 分钟自动转失败。
- **并发与角色容量**不足时任务**入队**，并记录排队原因。
- **资源锁**：避免特定资源冲突（实现于 `index.js`）。
- **研究归档目录**：初始化时确保存档目录存在（`data/research-archive`）。

---

## 七、插件设置页可配项（与 `plugin_config.json` 对应）

| 区域 | 功能 |
|------|------|
| **代理插件列表** | 勾选由世界之眼代管的插件；未勾选者仍直接暴露给主 AI。 |
| **模型分组** | 可复用的模型组；角色配置按组名引用。 |
| **角色配置** | 各角色的模型组与默认插件。 |
| **运行限制** | 并发任务上限、代码任务并发上限、LLM 超时与重试。 |
| **安全** | 代码执行开关、白名单、高危命令拦截。 |
| **世界之眼人格** | 统一工作人格开关（关闭则纯工程化提示；不含角色扮演与思考独白）。 |
| **协商交接** | 分身间交接追问澄清；**默认关闭以提速**，开启后每次追问多花 2-3 次模型调用。 |

---

## 八、依赖与运行环境

- 插件目录下 **`npm install`** 可提供 `node-fetch`（无内置 `fetch` 时的 HTTP 兼容）。
- 下级模型需支持 **工具/函数调用**。

---

若你只关心「怎么用」，可配合本目录 **README.md** 的快速开始；本文仅罗列**功能边界**，便于对照实现与排查问题。
